/* global describe, it */

'use strict'

const assert = require('assert')
const core = require('../lib/ikwin-core.js').default

describe('ikwin-core', function () {
    it('should just work', function () {
    })
})
